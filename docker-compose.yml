version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: genai_spec_engine_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: spec_engine_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: genai_spec_engine_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  postgres_data:
